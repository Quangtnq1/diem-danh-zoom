# Secret Token của Zoom App (dùng để trả lời CRC validation cho webhook).
# Lấy tại: marketplace.zoom.us -> App của bạn -> tab Feature/Webhook -> Secret Token.
# Trên Netlify: Site settings -> Environment variables -> thêm biến này.
ZOOM_WEBHOOK_SECRET_TOKEN=""
